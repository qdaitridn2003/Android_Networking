import { styled } from 'nativewind';
import { View } from 'react-native';

export const Container = styled(View, 'flex-1 p-3');
export const Row = styled(View, 'flex-row');
export const Col = styled(View, 'flex-col');
